[build]
builder = "nixpacks"

[deploy]
startCommand = "python scripts/bootstrap_model.py && uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
